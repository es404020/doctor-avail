import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'user',
  webDir: '../../dist/apps/user',
  bundledWebRuntime: false,
};

export default config;
